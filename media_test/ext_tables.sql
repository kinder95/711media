CREATE TABLE tt_content (
    header_mediatest varchar(255) DEFAULT '' NOT NULL,
    subheader_mediatest varchar(150) DEFAULT '',
    button_url  varchar(255) DEFAULT '',
    button_text varchar(30) DEFAULT ''
);